version: 2.1
jobs:
  run_tests:
    docker:
      - image: circleci/python:3.7

    steps:
      - checkout
      - run:
        name: Install Python depdencies
        command: pip install -r requirements.txt